; eslint-plugin-react-hooks@5 caps its peer range at ESLint 9, but works with
; the ESLint 10 flat config we use. Accept the lagging peer range so install
; (and CI's npm ci) resolves cleanly.
legacy-peer-deps=true
